/* CSV Export Utility */
function exportLaunchesCSV(launches,filename='launches.csv'){const headers=['ID','Name','Provider','Rocket','Status','NET','Location','Mission Type'];const rows=launches.map(l=>[l.id||'',l.name||'',l.provider||'',l.rocket||'',l.status||'',l.net||'',l.location||'',l.missionType||''].map(v=>`"${String(v).replace(/"/g,'""')}"`).join(','));const csv=[headers.join(','),...rows].join('\n');const blob=new Blob([csv],{type:'text/csv;charset=utf-8;'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=filename;a.click();URL.revokeObjectURL(url);}
